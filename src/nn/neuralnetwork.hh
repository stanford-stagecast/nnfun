#pragma once

#include <Eigen/Dense>
#include <iostream>

using namespace std;
using namespace Eigen;

template<class T, unsigned int batch_size, unsigned int num_layers, vector<unsigned int> layers>

class NeuralNetwork
{
}
